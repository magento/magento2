<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="../../../../../../vendor/magento/magento2-functional-testing-framework/src/Magento/FunctionalTestingFramework/Test/etc/testSchema.xsd">
    <test name="AdminFilterSourcesInGridTest">
        <annotations>
            <features value="Multi-Store Inventory"/>
            <stories value="Filter Sources in Grid"/>
            <title value="Filter Sources on the Manage Sources grid"/>
            <description value="You should be able to Filter the Sources from the Manage Sources grid."/>
            <testCaseId value="1408749"/>
            <group value="msi"/>
        </annotations>
        <before>
            <createData entity="TestSource1" stepKey="createStuff1"/>
            <createData entity="TestSource2" stepKey="createStuff2"/>
            <createData entity="TestSource1" stepKey="createStuff3"/>
            <createData entity="TestSource2" stepKey="createStuff4"/>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logoutOfAdmin"/>
        </after>

        <amOnPage url="{{ManageSources.url}}" stepKey="amOnTheSourcesListPage"/>
        <waitForPageLoad stepKey="waitForPageLoad1"/>

        <click selector="{{AdminGridFilterControls.filters}}" stepKey="clickOnFilters1"/>
        <fillField userInput="$$createStuff3.general[source_code]$$" selector="{{AdminGridFilterControls.code}}" stepKey="fillCodeField2"/>
        <click selector="{{AdminGridFilterControls.applyFilters}}" stepKey="clickOnApplyFilters1"/>

        <see userInput="$$createStuff3.general[source_code]$$" selector="{{AdminGrid.rowOne}}" stepKey="seeCodeInRow1"/>
        <see userInput="$$createStuff3.general[name]$$" selector="{{AdminGrid.rowOne}}" stepKey="seeNameInRow1"/>
        <see userInput="Enabled" selector="{{AdminGrid.rowOne}}" stepKey="seeIsEnabledInRow1"/>
        <!--Todo: MQE-677 needs to be merged into Develop before we can use "0" in the Data. Hardcoded "0" for now.-->
        <see userInput="0" selector="{{AdminGrid.rowOne}}" stepKey="seePriorityInRow1"/>
    </test>
</tests>
