<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="StorefrontLockCustomerOnLoginPageTest">
        <annotations>
            <features value="Customer"/>
            <title value="Lock customer on Storefront with after many attempts to log in with incorrect credentials"/>
            <description value="Lock customer on Storefront with after many attempts to log in with incorrect credentials"/>
            <group value="Customer"/>
            <group value="security"/>
            <group value="mtf_migrated"/>
        </annotations>
        <before>
            <magentoCLI command="config:set customer/captcha/enable 0" stepKey="disableCaptcha"/>
            <magentoCLI command="config:set customer/password/lockout_failures 5" stepKey="setInvalidAttemptsCount"/>
            <createData stepKey="customer" entity="Simple_US_Customer"/>
        </before>
        <after>
            <deleteData stepKey="deleteCustomer" createDataKey="customer" />
        </after>
        <!-- Perform 5 attempts to log in with invalid credentials -->
        <actionGroup ref="StorefrontCustomerLoginInvalidCredentialsActionGroup" stepKey="failedLoginAttempt1">
            <argument name="customerEmail" value="$$customer.email$$"/>
            <argument name="customerPassword" value="$$customer.password$$"/>
        </actionGroup>
        <actionGroup ref="StorefrontCustomerLoginInvalidCredentialsActionGroup" stepKey="failedLoginAttempt2">
            <argument name="customerEmail" value="$$customer.email$$"/>
            <argument name="customerPassword" value="$$customer.password$$"/>
        </actionGroup>
        <actionGroup ref="StorefrontCustomerLoginInvalidCredentialsActionGroup" stepKey="failedLoginAttempt3">
            <argument name="customerEmail" value="$$customer.email$$"/>
            <argument name="customerPassword" value="$$customer.password$$"/>
        </actionGroup>
        <actionGroup ref="StorefrontCustomerLoginInvalidCredentialsActionGroup" stepKey="failedLoginAttempt4">
            <argument name="customerEmail" value="$$customer.email$$"/>
            <argument name="customerPassword" value="$$customer.password$$"/>
        </actionGroup>
        <actionGroup ref="StorefrontCustomerLoginInvalidCredentialsActionGroup" stepKey="failedLoginAttempt5">
            <argument name="customerEmail" value="$$customer.email$$"/>
            <argument name="customerPassword" value="$$customer.password$$"/>
        </actionGroup>
        <!-- Make sure that the customer is locked -->
        <amOnPage stepKey="amOnSignInPage"  url="{{StorefrontCustomerSignInPage.url}}"/>
        <fillField  stepKey="fillEmail" userInput="$$customer.email$$" selector="{{StorefrontCustomerSignInFormSection.emailField}}"/>
        <fillField  stepKey="fillPassword" userInput="$$customer.password$$" selector="{{StorefrontCustomerSignInFormSection.passwordField}}"/>
        <click stepKey="clickSignInAccountButton" selector="{{StorefrontCustomerSignInFormSection.signInAccountButton}}"/>
        <waitForElementVisible selector="{{StorefrontCustomerLoginMessagesSection.errorMessage}}" stepKey="waitForFailMessage" />
        <see stepKey="seeErrorMessage" selector="{{StorefrontCustomerLoginMessagesSection.errorMessage}}" userInput="The account sign-in was incorrect or your account is disabled temporarily. Please wait and try again later."/>
    </test>
</tests>
