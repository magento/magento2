<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminSubmitsOrderPaymentMethodValidationTest">
        <annotations>
            <features value="Sales"/>
            <stories value="MC-5537: No UI validation for Payment methods when creating an order from admin"/>
            <title value="UI validation for Payment methods when creating an order from admin"/>
            <description value="Admin should not be able to submit orders without selecting a payment method when there is more than one"/>
            <severity value="AVERAGE"/>
            <testCaseId value="MC-6029"/>
            <group value="sales"/>
        </annotations>
        <before>
            <magentoCLI stepKey="allowSpecificValue" command="config:set payment/cashondelivery/active 1" />
            <createData entity="_defaultCategory" stepKey="createCategory"/>
            <createData entity="_defaultProduct" stepKey="createSimpleProduct">
                <requiredEntity createDataKey="createCategory"/>
            </createData>
            <actionGroup ref="AdminLoginActionGroup" stepKey="loginAsAdmin"/>
        </before>
        <after>
            <magentoCLI stepKey="allowSpecificValue" command="config:set payment/cashondelivery/active 0" />
            <deleteData createDataKey="createSimpleProduct" stepKey="deleteProduct"/>
            <deleteData createDataKey="createCategory" stepKey="deleteCategory"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="adminLogout"/>
        </after>
        <!--Create order via Admin-->
        <comment userInput="Admin creates order" stepKey="adminCreateOrderComment"/>
        <actionGroup ref="AdminOrdersPageOpenActionGroup" stepKey="navigateToOrderIndexPage"/>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeIndexPageTitle">
            <argument name="selector" value="{{AdminHeaderSection.pageTitle}}"/>
            <argument name="userInput" value="Orders"/>
        </actionGroup>
        <actionGroup ref="AdminClickCreateNewOrderOnOrderGridPageActionGroup" stepKey="clickCreateNewOrder"/>
        <actionGroup ref="AdminClickCreateNewCustomerForOrderActionGroup" stepKey="clickCreateCustomer"/>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeNewOrderPageTitle">
            <argument name="selector" value="{{AdminHeaderSection.pageTitle}}"/>
            <argument name="userInput" value="Create New Order"/>
        </actionGroup>

        <!--Check if order can be submitted without the required fields-->
        <actionGroup ref="AddSimpleProductToOrderActionGroup" stepKey="addSimpleProductToOrder" after="seeNewOrderPageTitle">
            <argument name="product" value="_defaultProduct"/>
        </actionGroup>
        <actionGroup ref="CheckRequiredFieldsNewOrderFormActionGroup" stepKey="checkRequiredFieldsNewOrder" after="addSimpleProductToOrder"/>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seePaymentMethodRequired" after="checkRequiredFieldsNewOrder">
            <argument name="selector" value="{{AdminOrderFormPaymentSection.paymentError}}"/>
            <argument name="userInput" value="Please select one of the options."/>
        </actionGroup>
        <scrollToTopOfPage stepKey="scrollToTopOfOrderFormPage" after="seePaymentMethodRequired"/>

        <!--Fill customer group and customer email-->
        <selectOption selector="{{AdminOrderFormAccountSection.group}}" userInput="{{GeneralCustomerGroup.code}}" stepKey="selectCustomerGroup" after="scrollToTopOfOrderFormPage"/>
        <fillField selector="{{AdminOrderFormAccountSection.email}}" userInput="{{Simple_US_Customer.email}}" stepKey="fillCustomerEmail" after="selectCustomerGroup"/>

        <!--Fill customer address information-->
        <actionGroup ref="FillOrderCustomerInformationActionGroup" stepKey="fillCustomerAddress" after="fillCustomerEmail">
            <argument name="customer" value="Simple_US_Customer"/>
            <argument name="address" value="US_Address_TX"/>
        </actionGroup>

        <!-- Select payment and shipping -->
        <waitForElementVisible selector="{{AdminOrderFormPaymentSection.checkMoneyOption}}" stepKey="waitForPaymentOptions"/>
        <selectOption selector="{{AdminOrderFormPaymentSection.checkMoneyOption}}" userInput="checkmo" stepKey="checkPaymentOption"/>
        <actionGroup ref="OrderSelectFlatRateShippingActionGroup" stepKey="selectFlatRateShipping" after="fillCustomerAddress"/>

        <!--Verify totals on Order page-->
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeOrderSubTotal" after="selectFlatRateShipping">
            <argument name="selector" value="{{AdminOrderFormTotalSection.total('Subtotal')}}"/>
            <argument name="userInput" value="${{AdminOrderSimpleProduct.subtotal}}"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeOrderShipping" after="seeOrderSubTotal">
            <argument name="selector" value="{{AdminOrderFormTotalSection.total('Shipping')}}"/>
            <argument name="userInput" value="${{AdminOrderSimpleProduct.shipping}}"/>
        </actionGroup>
        <scrollTo selector="{{AdminOrderFormTotalSection.grandTotal}}" stepKey="scrollToOrderGrandTotal"/>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeCorrectGrandTotal" after="scrollToOrderGrandTotal">
            <argument name="selector" value="{{AdminOrderFormTotalSection.grandTotal}}"/>
            <argument name="userInput" value="${{AdminOrderSimpleProduct.grandTotal}}"/>
        </actionGroup>
        <!--Submit Order and verify information-->
        <actionGroup ref="AdminOrderClickSubmitOrderActionGroup" stepKey="clickSubmitOrder" after="seeCorrectGrandTotal"/>
        <actionGroup ref="StorefrontAssertProperUrlIsShownActionGroup" stepKey="seeViewOrderPage" after="clickSubmitOrder">
            <argument name="urlPath" value="{{AdminOrderDetailsPage.url}}"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeSuccessMessage" after="seeViewOrderPage">
            <argument name="selector" value="{{AdminOrderDetailsMessagesSection.successMessage}}"/>
            <argument name="userInput" value="You created the order."/>
        </actionGroup>
    </test>
</tests>
