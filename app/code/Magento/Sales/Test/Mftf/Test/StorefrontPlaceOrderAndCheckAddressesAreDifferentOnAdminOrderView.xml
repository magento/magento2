<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="StorefrontPlaceOrderAndCheckAddressesAreDifferentOnAdminOrderView">
        <annotations>
            <stories value="Github Issue: #31608 Shipping address region leaking into billing address"/>
            <title value="Billing and Shipping addresses should show correct data on Admin Order View"/>
            <description value="Place order on Store Front with manually filled billing address state and selected shipping address state. Check that billing address show correct state on Admin Order View page"/>
            <severity value="MINOR"/>
            <group value="sales"/>
            <group value="GI31608"/>
        </annotations>
        <before>
            <!--Create simple product.-->
            <createData entity="SimpleProduct2" stepKey="createSimpleProduct"/>

            <!-- Create Customer Account -->
            <createData entity="Customer_UK_US" stepKey="createCustomer"/>

            <comment userInput="Adding the comment to replace 'indexer:reindex' command for preserving Backward Compatibility" stepKey="reindex"/>
            <comment userInput="Adding the comment to replace 'cache:flush' command for preserving Backward Compatibility" stepKey="flushCache"/>
        </before>
        <after>
            <deleteData createDataKey="createCustomer" stepKey="deleteCreateCustomer"/>
            <deleteData createDataKey="createSimpleProduct" stepKey="deleteSimpleProduct"/>

            <!-- Admin logout -->
            <actionGroup ref="AdminOrdersGridClearFiltersActionGroup" stepKey="clearFilters"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>

            <!-- Customer Logout-->
            <actionGroup ref="StorefrontCustomerLogoutActionGroup" stepKey="logoutCustomer"/>

            <!-- Reindex invalidated indices after product attribute has been created/deleted -->
            <magentoCron groups="index" stepKey="reindexInvalidatedIndices"/>
        </after>

        <!-- Order a product -->
        <amOnPage url="{{StorefrontProductPage.url($$createSimpleProduct.custom_attributes[url_key]$$)}}" stepKey="navigateToProductPage"/>
        <waitForPageLoad stepKey="waitForSimpleProductPageLoad"/>
        <actionGroup ref="StorefrontAddProductToCartActionGroup" stepKey="cartAddSimpleProductToCart">
            <argument name="product" value="$$createSimpleProduct$$"/>
            <argument name="productCount" value="1"/>
        </actionGroup>

        <!-- Got to checkout -->
        <actionGroup ref="GoToCheckoutFromMinicartActionGroup" stepKey="navigateToCheckout"/>
        <waitForPageLoad stepKey="waitForPaymentSelectionPageLoad"/>

        <!-- Login as customer on checkout page -->
        <actionGroup ref="LoginAsCustomerOnCheckoutPageActionGroup" stepKey="loginAsCustomer">
            <argument name="customer" value="$$createCustomer$$"/>
        </actionGroup>

        <!-- Go to payment section -->
        <waitForElement selector="{{CheckoutShippingSection.next}}" time="30" stepKey="waitForNextButton"/>
        <click selector="{{CheckoutShippingSection.next}}" stepKey="clickNext1"/>

        <!-- Place order -->
        <waitForElement selector="{{CheckoutPaymentSection.placeOrder}}" time="30" stepKey="waitForPlaceOrderButtonVisible"/>
        <click selector="{{CheckoutPaymentSection.placeOrder}}" stepKey="placeOrder"/>
        <waitForPageLoad stepKey="waitUntilOrderPlaced"/>

        <!-- Grab order number -->
        <seeElement selector="{{CheckoutSuccessMainSection.success}}" stepKey="orderIsSuccessfullyPlaced"/>
        <grabTextFrom selector="{{CheckoutSuccessMainSection.orderNumber22}}" stepKey="getOrderNumber"/>
        <assertNotEmpty stepKey="assertOrderIdIsNotEmpty" after="getOrderNumber">
            <actualResult type="const">$getOrderNumber</actualResult>
        </assertNotEmpty>

        <!-- Go to admin order view page -->
        <actionGroup ref="AdminLoginActionGroup" stepKey="loginAsAdmin"/>
        <actionGroup ref="AdminOrdersPageOpenActionGroup" stepKey="goToOrders"/>
        <actionGroup ref="OpenOrderByIdActionGroup" stepKey="filterOrderGridById">
            <argument name="orderId" value="$getOrderNumber"/>
        </actionGroup>

        <!-- Assert order addresses -->
        <see selector="{{AdminShipmentAddressInformationSection.shippingAddress}}" userInput="{{US_Address_NY_Default_Shipping.street[0]}}" stepKey="seeShippingAddressStreet"/>
        <see selector="{{AdminShipmentAddressInformationSection.shippingAddress}}" userInput="{{US_Address_NY_Default_Shipping.city}}" stepKey="seeShippingAddressCity"/>
        <see selector="{{AdminShipmentAddressInformationSection.shippingAddress}}" userInput="{{US_Address_NY_Default_Shipping.postcode}}" stepKey="seeShippingAddressPostcode"/>
        <see selector="{{AdminShipmentAddressInformationSection.shippingAddress}}" userInput="{{US_Address_NY_Default_Shipping.state}}" stepKey="seeShippingAddressState"/>
        <see selector="{{AdminShipmentAddressInformationSection.shippingAddress}}" userInput="{{US_Address_NY_Default_Shipping.telephone}}" stepKey="seeShippingAddressTelephone"/>

        <see selector="{{AdminShipmentAddressInformationSection.billingAddress}}" userInput="{{UK_With_State_Default_Billing.street[0]}}" stepKey="seeBillingAddressStreet"/>
        <see selector="{{AdminShipmentAddressInformationSection.billingAddress}}" userInput="{{UK_With_State_Default_Billing.city}}" stepKey="seeBillingAddressCity"/>
        <see selector="{{AdminShipmentAddressInformationSection.billingAddress}}" userInput="{{UK_With_State_Default_Billing.postcode}}" stepKey="seeBillingAddressPostcode"/>
        <see selector="{{AdminShipmentAddressInformationSection.billingAddress}}" userInput="{{UK_With_State_Default_Billing.state}}" stepKey="seeBillingAddressState"/>
        <see selector="{{AdminShipmentAddressInformationSection.billingAddress}}" userInput="{{UK_With_State_Default_Billing.telephone}}" stepKey="seeBillingAddressTelephone"/>

        <dontSee selector="{{AdminShipmentAddressInformationSection.billingAddress}}" userInput="{{US_Address_NY_Default_Shipping.state}}" stepKey="dontSeeShippingAddressStateAtBillingAddress"/>
    </test>
</tests>
