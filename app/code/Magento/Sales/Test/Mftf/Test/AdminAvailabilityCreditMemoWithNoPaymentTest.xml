<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminAvailabilityCreditMemoWithNoPaymentTest">
        <annotations>
            <features value="Sales"/>
            <stories value="MAGETWO-91547: Unable to create Credit memo for order with no payment required"/>
            <title value="Checking availability of 'Credit memo' button for order with no payment required"/>
            <description value="*Credit Memo* button should be displayed"/>
            <severity value="MAJOR"/>
            <testCaseId value="MAGETWO-94470"/>
            <group value="sales"/>
        </annotations>
        <before>
            <createData entity="_defaultCategory" stepKey="createCategory"/>
            <createData entity="_defaultProduct" stepKey="createProduct">
                <requiredEntity createDataKey="createCategory"/>
            </createData>
         
            <createData entity="FreeShippingMethodsSettingConfig" stepKey="freeShippingMethodsSettingConfig"/>
            <comment userInput="Adding the comment to replace CliCacheFlushActionGroup action group ('cache:flush' command) for preserving Backward Compatibility" stepKey="flushCache"/>
            <actionGroup ref="AdminLoginActionGroup" stepKey="loginAsAdmin"/>
        </before>
        <after>
        
            <createData entity="DefaultShippingMethodsConfig" stepKey="defaultShippingMethodsConfig"/>
            <createData entity="DisableFreeShippingConfig" stepKey="disableFreeShippingConfig"/>
            <deleteData createDataKey="createProduct" stepKey="deleteProduct"/>
            <deleteData createDataKey="createCategory" stepKey="deleteCategory"/>
            <actionGroup ref="AdminDeleteCustomerActionGroup" stepKey="deleteCustomer">
                <argument name="customerEmail" value="Simple_US_Customer.email"/>
            </actionGroup>
            <comment userInput="Adding the comment to replace CliCacheFlushActionGroup action group ('cache:flush' command) for preserving Backward Compatibility" stepKey="flushCache"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logOut"/>
        </after>

        <actionGroup ref="NavigateToNewOrderPageNewCustomerActionGroup" stepKey="navigateToNewOrderPage"/>

        <scrollToTopOfPage stepKey="scrollToTopOfOrderFormPage"/>
        <actionGroup ref="AddSimpleProductToOrderActionGroup" stepKey="addFirstProductToOrder">
            <argument name="product" value="$$createProduct$$"/>
        </actionGroup>

        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="clickCustomPriceCheckbox"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForPriceFieldAppears"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="fillCustomPriceField"/>
        <actionGroup ref="AdminSetCustomPriceOnCreateOrderPageActionGroup" stepKey="clickUpdateItemsAndQuantitiesButton"/>

        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="selectCustomerGroup"/>
        <actionGroup ref="AdminFillAccountInformationOnCreateOrderPageActionGroup" stepKey="fillCustomerEmail">
            <argument name="email" value="{{Simple_US_Customer.email}}"/>
        </actionGroup>

        <actionGroup ref="FillOrderCustomerInformationActionGroup" stepKey="fillCustomerAddress">
            <argument name="customer" value="Simple_US_Customer"/>
            <argument name="address" value="US_Address_TX"/>
        </actionGroup>

        <actionGroup ref="OrderSelectFreeShippingActionGroup" stepKey="selectFreeShippingOption"/>

        <actionGroup ref="AdminOrderClickSubmitOrderActionGroup" stepKey="clickSubmitOrder" />

        <actionGroup ref="StartCreateInvoiceFromOrderPageActionGroup" stepKey="startCreateInvoice"/>

        <actionGroup ref="SubmitInvoiceActionGroup" stepKey="submitInvoice"/>

        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="seeCreditMemo"/>  
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="clickCreditMemoItem"/>  
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForCreditMemoPageLoaded"/>  
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="seeNewMemoPageTitle"/>  
        <actionGroup ref="AdminStartCreateCreditMemoFromOrderPageActionGroup" stepKey="seeNewMemoUrlOnPage"/>
      
    </test>
</tests>
