<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="StorefrontCheckEmptyStateProvinceBillingTest">
        <annotations>
            <features value="Billing address"/>
            <stories value="State/province empty at billing address"/>
            <title value="Check if state/province is empty at billing address if it wasn't provided"/>
            <description value="Check if state/province is empty at billing address if it wasn't provided"/>
            <severity value="BLOCKER"/>
            <testCaseId value="MC-*"/>
        </annotations>
        <before>
            <createData entity="SimpleProduct2" stepKey="createSimpleProduct"/>
            <createData entity="Customer_DE_UK_US" stepKey="createCustomer"/>
        </before>
        <after>
            <actionGroup ref="AdminOrdersGridClearFiltersActionGroup" stepKey="clearFilters"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>
            <deleteData createDataKey="createSimpleProduct" stepKey="deleteSimpleProduct"/>
            <deleteData createDataKey="createCustomer" stepKey="deleteCustomer"/>
        </after>
        <!-- Add Simple Product to cart -->
        <amOnPage url="{{StorefrontProductPage.url($$createSimpleProduct.custom_attributes[url_key]$$)}}" stepKey="navigateToSimpleProductPage"/>
        <waitForPageLoad stepKey="waitForSimpleProductPageLoad"/>
        <actionGroup ref="AddToCartFromStorefrontProductPageActionGroup" stepKey="addToCartFromStorefrontProductPage">
            <argument name="productName" value="$$createSimpleProduct.name$$"/>
        </actionGroup>
        <!-- Go to shopping cart -->
        <actionGroup ref="ClickViewAndEditCartFromMiniCartActionGroup" stepKey="goToShoppingCartFromMiniCart"/>
        <actionGroup ref="StorefrontClickProceedToCheckoutActionGroup" stepKey="clickProceedToCheckout"/>
        <!-- Login as customer on checkout page -->
        <actionGroup ref="LoginAsCustomerOnCheckoutPageActionGroup" stepKey="loginAsCustomer">
            <argument name="customer" value="$$createCustomer$$"/>
        </actionGroup>
        <!-- Click next button to open payment section -->
        <click selector="{{CheckoutShippingGuestInfoSection.next}}" stepKey="clickNext"/>
        <waitForPageLoad stepKey="waitForShipmentPageLoad"/>
        <uncheckOption selector="{{CheckoutPaymentSection.billingAddressNotSameCheckbox}}" stepKey="sameBillingToNo"/>
        <!-- Change the address -->
        <waitForElementVisible selector="{{CheckoutPaymentSection.addressDropdown}}" stepKey="waitForDropDownToBeVisible"/>
        <selectOption selector="{{CheckoutShippingSection.addressDropdown}}" userInput="{{UK_Not_Default_Address.street[0]}}" stepKey="addAddress"/>
        <!-- Check order summary in checkout -->
        <click selector="{{CheckoutShippingSection.updateAddress}}" stepKey="clickToUpdate"/>
        <waitForPageLoad stepKey="waitForPageLoad"/>
        <!-- Place order -->
        <click selector="{{CheckoutPaymentSection.placeOrder}}" stepKey="clickPlaceOrder"/>
        <waitForPageLoad stepKey="waitForCheckoutPaymentSectionPageLoad"/>
        <seeElement selector="{{CheckoutSuccessMainSection.success}}" stepKey="orderIsSuccessfullyPlaced"/>
        <grabTextFrom selector="{{CheckoutSuccessMainSection.orderNumber22}}" stepKey="grabOrderNumber"/>
        <!-- Open created order in backend -->
        <actionGroup ref="AdminLoginActionGroup" stepKey="loginAsAdmin"/>
        <actionGroup ref="AdminOrdersPageOpenActionGroup" stepKey="goToOrders"/>
        <actionGroup ref="OpenOrderByIdActionGroup" stepKey="filterOrderGridById">
            <argument name="orderId" value="$grabOrderNumber"/>
        </actionGroup>
        <!-- Assert order billing address without state/province from shipping address-->
        <see selector="{{AdminShipmentAddressInformationSection.shippingAddress}}" userInput="{{US_Address_NY.state}}" stepKey="seeShippingAddressState"/>
        <dontSee selector="{{AdminShipmentAddressInformationSection.billingAddress}}" userInput="{{US_Address_NY.state}}" stepKey="dontSeeBillingAddressState"/>
    </test>
</tests>
