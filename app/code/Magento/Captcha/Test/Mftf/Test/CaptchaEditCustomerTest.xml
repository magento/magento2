<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="CaptchaEditCustomerEmailTest">
        <annotations>
            <features value="Captcha"/>
            <stories value="Check captcha and lockout customer on the account edit page."/>
            <title value="Test for checking captcha on the customer account edit page and customer is locked."/>
            <description value="Test for checking captcha on the customer account edit page and customer is locked."/>
            <severity value="MAJOR"/>
            <group value="captcha"/>
            <group value="mtf_migrated"/>
        </annotations>
        <before>
            <magentoCLI command="config:set customer/captcha/forms user_edit" stepKey="enableUserEditCaptcha"/>
            <magentoCLI command="cache:clean config full_page" stepKey="cleanInvalidatedCaches1"/>
            <createData entity="Simple_US_Customer" stepKey="customer"/>
            <!-- Sign in as customer -->
            <actionGroup ref="LoginToStorefrontActionGroup" stepKey="loginToStorefrontAccount">
                <argument name="Customer" value="$$customer$$"/>
            </actionGroup>
        </before>
        <after>
            <deleteData createDataKey="customer" stepKey="deleteCustomer"/>
            <!-- Revert Captcha forms configurations -->
            <magentoCLI command="config:set customer/captcha/forms user_login,user_forgotpassword" stepKey="enableUserEditCaptcha"/>
            <magentoCLI command="cache:clean config full_page" stepKey="cleanInvalidatedCaches1"/>
        </after>

        <!-- Open Customer edit page -->
        <amOnPage url="/customer/account/edit/" stepKey="goToEditPage"/>
        <waitForPageLoad stepKey="waitForEditPage"/>

        <!-- Update email with incorrect password 3 times. -->
        <click selector="{{StorefrontCustomerAccountInformationSection.changeEmail}}" stepKey="clickChangeEmailCheckbox1"/>
        <fillField selector="{{StorefrontCustomerAccountInformationSection.currentPassword}}" userInput="incorrectPassword" stepKey="enterWrongPassword1"/>
        <click selector="{{StorefrontCustomerAccountInformationSection.saveButton}}" stepKey="saveChange1"/>
        <see userInput="The password doesn't match this account. Verify the password and try again." selector="{{StorefrontCustomerMessagesSection.errorMessage}}" stepKey="verifyMessage1"/>

        <click selector="{{StorefrontCustomerAccountInformationSection.changeEmail}}" stepKey="clickChangeEmailCheckbox2"/>
        <fillField selector="{{StorefrontCustomerAccountInformationSection.currentPassword}}" userInput="incorrectPassword" stepKey="enterWrongPassword2"/>
        <click selector="{{StorefrontCustomerAccountInformationSection.saveButton}}" stepKey="saveChange2"/>
        <see userInput="The password doesn't match this account. Verify the password and try again." selector="{{StorefrontCustomerMessagesSection.errorMessage}}" stepKey="verifyMessage2"/>

        <click selector="{{StorefrontCustomerAccountInformationSection.changeEmail}}" stepKey="clickChangeEmailCheckbox3"/>
        <fillField selector="{{StorefrontCustomerAccountInformationSection.currentPassword}}" userInput="incorrectPassword" stepKey="enterWrongPassword3"/>
        <click selector="{{StorefrontCustomerAccountInformationSection.saveButton}}" stepKey="saveChange3"/>
        <see userInput="The password doesn't match this account. Verify the password and try again." selector="{{StorefrontCustomerMessagesSection.errorMessage}}" stepKey="verifyMessage3"/>

        <!-- Check captcha visibility after incorrect password submit form -->
        <waitForElementVisible selector="{{StorefrontCustomerEditFormSection.captchaField}}" stepKey="seeCaptchaField"/>
        <waitForElementVisible selector="{{StorefrontCustomerEditFormSection.captchaImg}}" stepKey="seeCaptchaImage"/>
        <waitForElementVisible selector="{{StorefrontCustomerEditFormSection.captchaReload}}" stepKey="seeCaptchaReloadButton"/>
        <reloadPage stepKey="refreshPage"/>
        <waitForPageLoad stepKey="waitForPageLoad2"/>
        <waitForElementVisible selector="{{StorefrontCustomerEditFormSection.captchaField}}" stepKey="seeCaptchaField2"/>
        <waitForElementVisible selector="{{StorefrontCustomerEditFormSection.captchaImg}}" stepKey="seeCaptchaImage2"/>
        <waitForElementVisible selector="{{StorefrontCustomerEditFormSection.captchaReload}}" stepKey="seeCaptchaReloadButton2"/>

        <!-- Try to submit for with incorrect captcha -->
        <click selector="{{StorefrontCustomerAccountInformationSection.changeEmail}}" stepKey="clickChangeEmailCheckbox4"/>
        <fillField selector="{{StorefrontCustomerAccountInformationSection.currentPassword}}" userInput="incorrectPassword" stepKey="enterWrongPassword4"/>
        <fillField selector="{{StorefrontCustomerEditFormSection.captchaField}}" userInput="wrongCaptcha" stepKey="enterWrongCaptcha"/>
        <click selector="{{StorefrontCustomerAccountInformationSection.saveButton}}" stepKey="saveChange4"/>
        <see userInput="Incorrect CAPTCHA" selector="{{StorefrontCustomerMessagesSection.errorMessage}}" stepKey="verifyMessage4"/>
    </test>
</tests>
