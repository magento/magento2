<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->
    ​
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="ConfigurationCheckTest">
        <annotations>
            <stories value="Dynamic Media URL1"/>
            <title value="Verify that Allow Dynamic Media URLs setting is removed from configuration page1"/>
            <description value="Verify that Allow Dynamic Media URLs setting is removed from configuration page1"/>
            <severity value="CRITICAL"/>
            <testCaseId value="MC-31781"/>
        </annotations>
        <before>
            <actionGroup ref="AdminLoginActionGroup" stepKey="loginAsAdmin"/>
        </before>
        <after>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>
        </after>
        <amOnPage url="{{CatalogConfigPage.url}}" stepKey="navigateToConfigurationPage" />
        <waitForPageLoad stepKey="waitForPageLoad"/>
        <conditionalClick stepKey="expandStorefrontTab" selector="{{CatalogSection.storefront}}" dependentSelector="{{CatalogSection.CheckIfTabExpand}}" visible="true" />
        <dontSee stepKey="dontSeeDynamicMediaURLsSetting" userInput="Allow Dynamic Media URLs"/>
    </test>
</tests>
