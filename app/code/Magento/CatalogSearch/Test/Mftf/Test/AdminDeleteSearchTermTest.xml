<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminDeleteSearchTermTest">
        <annotations>
            <stories value="DeleteSearchTerm"/>
            <title value="DeleteSearchTermEntityTestVariation1"/>
            <description value="Test log in to SearchTerm and DeleteSearchTerm"/>
            <testCaseId value="MC-13988"/>
            <severity value="CRITICAL"/>
            <group value="CatalogSearch"/>
            <group value="mtf_migrated"/>
        </annotations>

        <before>
            <actionGroup ref = "LoginAsAdmin" stepKey="loginAsAdmin"/>
            <createData entity="SimpleSubCategory" stepKey="initialCategoryEntity"/>
            <createData entity="SimpleProduct" stepKey="simpleProduct">
                <requiredEntity createDataKey="initialCategoryEntity"/>
            </createData>
        </before>
        <after>
            <deleteData stepKey="deleteSimpleSubCategory" createDataKey="initialCategoryEntity"/>
            <deleteData stepKey="deleteSimpleProduct" createDataKey="simpleProduct"/>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>

        <!--Add new search term-->
        <actionGroup ref="AssertSearchTermSaveSuccessMessage" stepKey="addNewSearchTerm">
            <argument name="searchQuery" value="{{SimpleTerm.search_query}}"/>
            <argument name="storeValue" value="Default Store View"/>
            <argument name="redirectUrl" value="{{SimpleTerm.redirect}}"/>
            <argument name="displayInSuggestedTerm" value="No"/>
        </actionGroup>

        <!--Search and delete search term and AssertSearchTermSuccessDeleteMessage-->
        <actionGroup ref="AssertSearchTermSuccessDeleteMessage" stepKey="deleteSearchTerm">
            <argument name="searchQuery" value="{{SimpleTerm.search_query}}"/>
        </actionGroup>

        <!--Verify deleted search term in grid and AssertSearchTermNotInGrid-->
        <actionGroup ref="AssertSearchTermNotInGrid" stepKey="verifyDeletedSearchTermNotInGrid">
            <argument name="searchQuery" value="{{SimpleTerm.search_query}}"/>
        </actionGroup>

        <!--Verify AssertSearchTermNotOnFrontend-->
        <actionGroup ref="AssertSearchTermNotOnFrontend" stepKey="verifySearchTermNotOnFrontend">
            <argument name="searchQuery" value="{{SimpleTerm.search_query}}"/>
            <argument name="url_key" value="$$simpleProduct.custom_attributes[url_key]$$"/>
        </actionGroup>
    </test>
</tests>