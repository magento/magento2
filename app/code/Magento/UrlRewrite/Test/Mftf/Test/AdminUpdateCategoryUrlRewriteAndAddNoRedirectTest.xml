<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminUpdateCategoryUrlRewriteAndAddNoRedirectTest">
        <annotations>
            <stories value="Update URL rewrite"/>
            <title value="Update Category URL Rewrites, no redirect type"/>
            <description value="Login as Admin and update category Url Rewrite and add redirect type No"/>
            <testCaseId value="MC-5355"/>
            <severity value="CRITICAL"/>
            <group value="mtf_migrated"/>
        </annotations>

        <before>
            <createData entity="SimpleSubCategory" stepKey="category"/>
            <actionGroup ref="LoginAsAdmin" stepKey="login"/>
        </before>
        <after>
            <deleteData createDataKey="category" stepKey="deleteCategory"/>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>

        <!-- Search and Select Edit option for created category in grid -->
        <actionGroup ref="AdminSearchAndSelectUrlRewriteInGrid" stepKey="editUrlRewrite">
            <argument name="requestPath" value="$$category.custom_attributes[url_key]$$.html"/>
        </actionGroup>

        <!-- Open UrlRewrite Edit page and update the fields  -->
        <actionGroup ref="AdminUpdateUrlRewrite" stepKey="updateCategoryUrlRewrite">
            <argument name="storeValue" value="Default Store View"/>
            <argument name="requestPath" value="{{defaultUrlRewrite.request_path}}"/>
            <argument name="redirectTypeValue" value="No"/>
            <argument name="description" value="Update Category Url Rewrite"/>
        </actionGroup>

        <!-- Open Category Page and Get Category ID -->
        <actionGroup ref="OpenCategoryFromCategoryTree" stepKey="getCategoryId">
            <argument name="category"  value="$$category.name$$"/>
        </actionGroup>
        <grabFromCurrentUrl stepKey="categoryId" regex="#\/([0-9]*)?\/$#"/>

        <!-- Assert category Url Rewrite in grid -->
        <actionGroup ref="AdminSearchByRequestPath" stepKey="searchByNewRequestPath">
            <argument name="redirectPath" value="{{defaultUrlRewrite.request_path}}" />
            <argument name="redirectType" value="No" />
            <argument name="targetPath" value="catalog/category/view/id/{$categoryId}"/>
        </actionGroup>

        <!-- Assert Category redirect in Store Front -->
        <actionGroup ref="AssertStorefrontUrlRewriteRedirect" stepKey="verifyCategoryInStoreFront">
            <argument name="category" value="$$category.name$$"/>
            <argument name="newRequestPath" value="{{defaultUrlRewrite.request_path}}"/>
        </actionGroup>
    </test>
</tests>
