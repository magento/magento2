<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../../../../../../vendor/magento/magento2-functional-testing-framework/src/Magento/FunctionalTestingFramework/Test/etc/testSchema.xsd">
    <test name="AdminCreateNewStockWithNoSourceOrSalesChannelTest">
        <annotations>
            <features value="Multi-Source Inventory"/>
            <stories value="Create New Stock"/>
            <title value="New Stock created by Admin user"/>
            <description value="You should be able to create a New Stock, via the Admin, without adding a Source or Sales Channel."/>
            <testCaseId value="513"/>
            <group value="msi"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
        </before>
        <after>
            <actionGroup ref="LogoutOfAdmin" stepKey="logoutOfAdmin"/>
        </after>

        <amOnPage url="{{ManageStock.url}}" stepKey="amOnTheStockListPage"/>
        <waitForPageLoad time="30" stepKey="waitForPageLoad1"/>
        <click selector="{{AdminGridMainControls.add}}" stepKey="clickOnAdd"/>

        <fillField userInput="{{_defaultStock.name}}" selector="{{AdminEditStockGeneralSection.name}}" stepKey="fillName"/>

        <actionGroup ref="AdminSaveAndClose" stepKey="saveAndClose1"/>

        <actionGroup ref="AdminGridSearchByKeyword" stepKey="searchByKeyword">
            <argument name="value" value="_defaultStock.name"/>
        </actionGroup>

        <see userInput="{{_defaultStock.name}}" selector="{{AdminGridRow.rowOne}}" stepKey="seeCodeInRow1"/>
    </test>
</tests>
