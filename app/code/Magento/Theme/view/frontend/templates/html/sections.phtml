<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

// @codingStandardsIgnoreFile

?>
<?php

/**
*  General template for displaying group of blocks devided into sections
*/

$group = $block->getGroupName();
$groupCss = $block->getGroupCss();
$groupBehavior = $block->getGroupBehaviour() ? $block->getGroupBehaviour() : '{"tabs":{"openedState":"active"}}';
?>
<?php if ($detailedInfoGroup = $block->getGroupChildNames($group, 'getChildHtml')):?>
    <div class="sections <?php echo /* @escapeNotVerified */ $groupCss ?>">
        <?php $layout = $block->getLayout(); ?>
        <div class="section-items <?php echo /* @escapeNotVerified */ $groupCss ?>-items" data-mage-init='<?php echo /* @escapeNotVerified */ $groupBehavior ?>'>
            <?php foreach ($detailedInfoGroup as $name):?>
                <?php
                    $html = $layout->renderElement($name);
                    if (!trim($html) && ($block->getUseForce() != true)) {
                        continue;
                    }
                    $alias = $layout->getElementAlias($name);
                    $label = $block->getChildData($alias, 'title');
                ?>
                <div class="section-item-title <?php echo /* @escapeNotVerified */ $groupCss ?>-item-title" data-role="collapsible">
                    <a class="<?php echo /* @escapeNotVerified */ $groupCss ?>-item-switch" data-toggle="switch" href="#<?php echo /* @escapeNotVerified */ $alias ?>"><?php echo /* @escapeNotVerified */ $label ?></a>
                </div>
                <div class="section-item-content <?php echo /* @escapeNotVerified */ $groupCss ?>-item-content" id="<?php echo /* @escapeNotVerified */ $alias ?>" data-role="content"><?php echo /* @escapeNotVerified */ $html ?></div>
            <?php endforeach;?>
        </div>
    </div>
<?php endif; ?>
