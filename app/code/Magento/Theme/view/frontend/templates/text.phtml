<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

$attributes = $block->getCssClass() ? ' class="' . $escaper->escapeHtmlAttr($block->getCssClass()) . '"' : '';
$attr = $block->getAttributes();
if (!empty($attr)) {
    foreach ($block->getAttributes() as $attribute => $value) {
        $attributes .= ' ' . $escaper->escapeHtml($attribute) . '="' . $escaper->escapeHtmlAttr($value) . '"';
    }
}
/* @noEscape */ echo '<'
    . $escaper->escapeHtml($block->getTag())
    . $attributes
    . '>'
    . $escaper->escapeHtml($block->getText())
    . '</'
    . $escaper->escapeHtml($block->getTag())
    . '>';
