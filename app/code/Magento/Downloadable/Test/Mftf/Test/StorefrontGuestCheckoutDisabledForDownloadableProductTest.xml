<?xml version="1.0" encoding="UTF-8"?>
<!--
/**
 * Copyright 2026 Adobe
 * All Rights Reserved.
 */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="StorefrontGuestCheckoutDisabledForDownloadableProductTest">
        <annotations>
            <features value="Downloadable"/>
            <stories value="Guest Checkout with Downloadable Products"/>
            <title value="Sign In modal is shown when guest checkout is disabled for downloadable products"/>
            <description value="With global guest checkout enabled and catalog/downloadable/disable_guest_checkout enabled, guest cannot checkout downloadable cart and sees Sign In modal."/>
            <severity value="MAJOR"/>
            <group value="Downloadable"/>
        </annotations>
        <before>
            <!-- Allow guest checkout globally so only downloadable-specific config is under test -->
            <createData entity="EnableAllowGuestCheckout" stepKey="enableGlobalGuestCheckout"/>
            <magentoCLI command="config:set {{DisableGuestCheckoutWithDownloadableItems.path}} {{DisableGuestCheckoutWithDownloadableItems.value}}" stepKey="disableGuestCheckoutWithDownloadableItems"/>
            <magentoCLI stepKey="addDownloadableDomain" command="downloadable:domains:add example.com static.magento.com"/>
            <createData entity="_defaultCategory" stepKey="createCategory"/>
            <createData entity="DownloadableProductWithOneLink" stepKey="createProduct">
                <requiredEntity createDataKey="createCategory"/>
            </createData>
        </before>
        <after>
            <magentoCLI stepKey="removeDownloadableDomain" command="downloadable:domains:remove example.com static.magento.com"/>
            <deleteData stepKey="deleteProduct" createDataKey="createProduct"/>
            <deleteData stepKey="deleteCategory" createDataKey="createCategory"/>
        </after>

        <!-- Step 1: Open downloadable product PDP as guest -->
        <amOnPage url="{{StorefrontProductPage.url($$createProduct.custom_attributes[url_key]$$)}}" stepKey="amOnStorefrontProductPage"/>
        <waitForPageLoad stepKey="waitForProductPageLoad"/>

        <!-- Step 2: Add downloadable product to cart -->
        <actionGroup ref="StorefrontAddProductToCartActionGroup" stepKey="addProductToCart">
            <argument name="product" value="$$createProduct$$"/>
            <argument name="productCount" value="1"/>
        </actionGroup>

        <!-- Step 3: Proceed to checkout from mini cart -->
        <actionGroup ref="GoToCheckoutFromMinicartActionGroup" stepKey="goToCheckoutFromMinicart"/>

        <!-- Step 4: Sign In modal is shown instead of guest checkout -->
        <seeElement selector="{{StorefrontCustomerSignInPopupFormSection.email}}" stepKey="assertEmailFieldIsPresent"/>
        <seeElement selector="{{StorefrontCustomerSignInPopupFormSection.password}}" stepKey="assertPasswordFieldIsPresent"/>
        <seeElement selector="{{StorefrontCustomerSignInPopupFormSection.createAnAccount}}" stepKey="assertCreateAnAccountButtonIsPresent"/>
        <seeElement selector="{{StorefrontCustomerSignInPopupFormSection.forgotYourPassword}}" stepKey="assertForgotYourPasswordLinkIsPresent"/>
    </test>
</tests>
