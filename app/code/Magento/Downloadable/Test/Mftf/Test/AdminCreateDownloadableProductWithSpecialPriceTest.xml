<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminCreateDownloadableProductWithSpecialPriceTest" extends="AdminCreateDownloadableProductWithGroupPriceTest">
        <annotations>
            <features value="Catalog"/>
            <stories value="Create Downloadable Product"/>
            <title value="Create downloadable product with special price test"/>
            <description value="Admin should be able to create downloadable product with special price"/>
            <severity value="CRITICAL"/>
            <testCaseId value="MC-14513"/>
            <group value="Downloadable"/>
            <group value="mtf_migrated"/>
        </annotations>
        <remove keyForRemoval="addCustomerGroupPrice"/>

        <!-- Add special price to product -->
        <actionGroup ref="AddSpecialPriceToProductActionGroup" stepKey="setSpecialPriceToCreatedProduct" after="createProduct">
            <argument name="price" value="{{specialProductPrice.price}}"/>
        </actionGroup>

        <remove keyForRemoval="grabGroupPrice"/>
        <remove keyForRemoval="assertGroupPrice"/>

        <!-- Assert special price in storefront product page -->
        <grabTextFrom selector="{{StorefrontProductInfoMainSection.specialPriceValue}}" stepKey="grabSpecialPrice" after="AssertProductInStorefrontProductPage"/>
        <assertEquals expected='${{specialProductPrice.price}}' expectedType="string" actual="$grabSpecialPrice" stepKey="assertSpecialPrice"/>
    </test>
</tests>
