<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

// @codingStandardsIgnoreFile

/** @var \Magento\Shipping\Block\Adminhtml\Create\Items $block */
?>

<section class="admin__page-section">
    <div class="admin__data-grid-wrap">
        <table class="data-grid order-shipment-table">
            <thead>
                <tr>
                    <th class="data-grid-th col-product">
                        <span><?= $block->escapeHtml(__('Product')) ?></span>
                    </th>
                    <th class="data-grid-th col-ordered-qty">
                        <span><?= $block->escapeHtml(__('Qty')) ?></span>
                    </th>
                    <th class="data-grid-th col-qty">
                        <span><?= /* @escapeNotVerified */ __('Qty to Ship') ?></span>
                    </th>
                    <!-- TODO: Add logic for Partially Items -->
                    <?php if (!$block->canShipPartiallyItem()): ?>
                    <th class="col-ship last"><span><?= /* @escapeNotVerified */ __('Ship') ?></span></th>
                    <?php endif; ?>
                </tr>
            </thead>
            <?php $_items = $block->getShipment()->getAllItems() ?>
            <?php $_i = 0; foreach ($_items as $_item): if ($_item->getOrderItem()->getParentItem()): continue; endif; $_i++ ?>
                <tbody>
                    <?= $block->getItemHtml($_item) ?>
                </tbody>
            <?php endforeach; ?>
        </table>
    </div>
</section>
