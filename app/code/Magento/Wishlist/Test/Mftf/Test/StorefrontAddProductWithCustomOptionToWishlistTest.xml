<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="StorefrontAddProductWithCustomOptionToWishlistTest">
        <annotations>
            <stories value="Customer adds simple product with custom options to wishlist"/>
            <title value="Customer should to add product with custom option to wishlist and sto see success message"/>
            <description value="Customer should to add product with custom option to wishlist and sto see success message"/>
            <group value="Wishlist"/>
        </annotations>
        <before>
            <createData entity="_defaultCategory" stepKey="createCategory"/>
            <createData entity="_defaultProduct" stepKey="createSimpleProduct">
                <requiredEntity createDataKey="createCategory"/>
            </createData>
            <updateData createDataKey="createSimpleProduct" entity="productWithCustomOptions" stepKey="updateProductWithOptions1"/>
            <createData entity="Simple_US_Customer" stepKey="createCustomer"/>
        </before>
        <after>
            <deleteData createDataKey="createSimpleProduct" stepKey="deleteProduct"/>
            <deleteData createDataKey="createCategory" stepKey="deleteCategory"/>
            <deleteData createDataKey="createCustomer" stepKey="deleteCustomer"/>
            <actionGroup ref="StorefrontCustomerLogoutActionGroup" stepKey="logoutFromStorefront"/>
        </after>
        <actionGroup ref="LoginToStorefrontActionGroup" stepKey="customerLogin">
            <argument name="Customer" value="$$createCustomer$$" />
        </actionGroup>
        <actionGroup ref="StorefrontOpenProductPageActionGroup" stepKey="openProductStorefront">
            <argument name="productUrl" value="$$createSimpleProduct.custom_attributes[url_key]$$"/>
        </actionGroup>
        <actionGroup ref="StorefrontAddSimpleProductWithCustomOptionToWishlistActionGroup" stepKey="addProductToWishlist"/>
        <actionGroup ref="AssertStorefrontAddProductToWishlistMessageActionGroup" stepKey="assertSuccessMessage">
            <argument name="productName" value="$$createSimpleProduct.name$$"/>
        </actionGroup>
    </test>
</tests>
