<?xml version="1.0" encoding="UTF-8"?>
<!--
/**
 * Copyright 2020 Adobe
 * All Rights Reserved.
 */
-->

<actionGroups xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/actionGroupSchema.xsd">
    <actionGroup name="AssertStorefrontProductPageGalleryChangingFullscreenImageByRibbonActionGroup">
        <annotations>
            <description>On the product page change main image by clicking on the images in the ribbon. Fullscreen</description>
        </annotations>
        <arguments>
            <argument name="startImage" type="string" />
            <argument name="expectedImage" type="string" />
        </arguments>
        <waitForElementVisible selector="{{StorefrontProductMediaSection.productImageFullscreen(startImage)}}" stepKey="seeStartFullscreenImage"/>
        <waitForElementVisible selector="{{StorefrontProductMediaSection.imgSelectedInThumbnail(startImage)}}" stepKey="seeActiveImageInThumbnail"/>
        <waitForElementClickable selector="{{StorefrontProductMediaSection.productImageInFotorama(expectedImage)}}" stepKey="waitForClickOnExpectedImage"/>
        <click selector="{{StorefrontProductMediaSection.productImageInFotorama(expectedImage)}}" stepKey="clickOnExpectedImage"/>
        <waitForElementVisible selector="{{StorefrontProductMediaSection.productImageFullscreen(expectedImage)}}" stepKey="seeExpectedImageAfterClick"/>
        <waitForElementVisible selector="{{StorefrontProductMediaSection.imgSelectedInThumbnail(expectedImage)}}" stepKey="seeExpectedImageActiveInThumbnailAfterChange"/>
        <waitForElementClickable selector="{{StorefrontProductMediaSection.productImageInFotorama(startImage)}}" stepKey="waitForClickOnStartImageInRibbon"/>
        <click selector="{{StorefrontProductMediaSection.productImageInFotorama(startImage)}}" stepKey="clickOnStartImageInRibbon"/>
        <waitForElementVisible selector="{{StorefrontProductMediaSection.productImageFullscreen(startImage)}}" stepKey="seeStartImageAfterSecondChange"/>
        <waitForElementVisible selector="{{StorefrontProductMediaSection.imgSelectedInThumbnail(startImage)}}" stepKey="seeStartImageActiveInThumbnailAfterSecondChange"/>
    </actionGroup>
</actionGroups>
