<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminMoveCategoryAndCheckUrlRewritesTest">
        <annotations>
            <stories value="Move categories"/>
            <title value="URL Rewrites for subcategories during creation and move"/>
            <description value="Login as admin, move category from one to another and check category url rewrites"/>
            <testCaseId value="MC-6494"/>
            <features value="Catalog"/>
            <severity value="BLOCKER"/>
            <group value="mtf_migrated"/>
        </annotations>
        <before>
            <actionGroup ref="AdminLoginActionGroup" stepKey="loginToAdminPanel"/>
            <createData entity="FirstLevelSubCat" stepKey="createDefaultCategory">
                <field key="is_active">true</field>
            </createData>
            <magentoCron groups="index" stepKey="reindex"/>
            <actionGroup ref="CliCacheFlushActionGroup" stepKey="flushCache">
                <argument name="tags" value=""/>
            </actionGroup>
        </before>
        <after>
            <deleteData createDataKey="createDefaultCategory" stepKey="deleteCategory"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>
        </after>

        <!--Open category page-->
        <actionGroup ref="AdminOpenCategoryPageActionGroup" stepKey="openAdminCategoryIndexPage"/>
        <actionGroup ref="AdminExpandCategoryTreeActionGroup" stepKey="clickOnExpandTree"/>
        <actionGroup ref="AdminCategoriesOpenCategoryActionGroup" stepKey="selectCategory">
            <argument name="category" value="$$createDefaultCategory$$"/>
        </actionGroup>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForPageToLoad"/>

        <!--Create second level category-->
        <click selector="{{AdminCategorySidebarActionSection.AddSubcategoryButton}}" stepKey="clickOnAddSubCategoryButton"/>
        <actionGroup ref="AdminChangeCategoryNameActionGroup" stepKey="addSubCategoryName">
            <argument name="categoryName" value="{{SubCategory.name}}"/>
        </actionGroup>
        <actionGroup ref="AdminSaveCategoryActionGroup" stepKey="saveSubCategory1"/>
        <actionGroup ref="AssertMessageInAdminPanelActionGroup" stepKey="seeSuccessMessage1">
            <argument name="message" value="You saved the category."/>
        </actionGroup>

        <!--Create third level category under second level category-->
        <click selector="{{AdminCategorySidebarActionSection.AddSubcategoryButton}}" stepKey="clickOnAddSubCategoryButton1"/>
        <actionGroup ref="AdminChangeCategoryNameActionGroup" stepKey="addSubCategoryName1">
            <argument name="categoryName" value="{{SimpleSubCategory.name}}"/>
        </actionGroup>
        <actionGroup ref="AdminSaveCategoryActionGroup" stepKey="saveSubCategory2"/>
        <actionGroup ref="AssertMessageInAdminPanelActionGroup" stepKey="seeSuccessMessage2">
            <argument name="message" value="You saved the category."/>
        </actionGroup>
        <grabFromCurrentUrl stepKey="categoryId" regex="#\/([0-9]*)?\/$#" />

        <!--Open Url Rewrite Page-->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="openUrlRewriteIndexPage"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForUrlRewritePage"/>

        <!--Search third level category Redirect Path, Target Path and Redirect Type-->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="openUrlRewriteGridFilters"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="fillRequestPathFilter"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="clickOrderApplyFilters"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForPageToLoad0"/>

        <!--Verify Category RedirectType-->
        <actionGroup ref="AdminSearchByRequestPathActionGroup" stepKey="verifyTheRedirectType">
            <argument name="redirectPath" value="{{FirstLevelSubCat.name_lwr}}2/{{SubCategory.name_lwr}}/{{SimpleSubCategory.name_lwr}}.html" />
            <argument name="redirectType" value="No" />
            <argument name="targetPath" value="catalog/category/view/id/{$categoryId}"/>
        </actionGroup>

        <!--Verify Redirect Path -->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="verifyTheRedirectPath"/>

        <!--Verify Category Target Path-->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="verifyTheTargetPath"/>

        <!--Open Category Page -->
        <actionGroup ref="AdminOpenCategoryPageActionGroup" stepKey="openAdminCategoryIndexPage1"/>
        <actionGroup ref="AdminExpandCategoryTreeActionGroup" stepKey="clickOnExpandTree2"/>

        <!--Move the third level category under first level category -->
        <dragAndDrop selector1="{{AdminCategorySidebarTreeSection.categoryInTree(SimpleSubCategory.name)}}" selector2="{{AdminCategorySidebarTreeSection.categoryInTree($$createDefaultCategory.name$$)}}" stepKey="m0oveCategory"/>
        <see selector="{{AdminCategoryModalSection.message}}" userInput="This operation can take a long time" stepKey="seeWarningMessage"/>
        <click selector="{{AdminCategoryModalSection.ok}}" stepKey="clickOkButtonOnWarningPopup"/>
        <waitForPageLoad stepKey="waitForPageToLoad3"/>
        <see selector="{{AdminCategoryMessagesSection.SuccessMessage}}" userInput="You moved the category." stepKey="seeSuccessMoveMessage"/>

        <!--Open Url Rewrite page  -->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="openUrlRewriteIndexPage1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForUrlRewritePage1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="openUrlRewriteGridFilters1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="fillCategoryUrlKey1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="clickOrderApplyFilters1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForPageToLoad4"/>

        <!--Verify new Redirect Path after move -->
        <actionGroup ref="AdminSearchByRequestPathActionGroup" stepKey="verifyTheRequestPathAfterMove">
            <argument name="redirectPath" value="{{FirstLevelSubCat.name_lwr}}2/{{SimpleSubCategory.name_lwr}}.html" />
            <argument name="redirectType" value="No" />
            <argument name="targetPath" value="catalog/category/view/id/{$categoryId}"/>
        </actionGroup>

        <!--Verify new Target Path after move -->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="verifyTheTargetPathAfterMove"/>
        <!--Verify new RedirectType after move -->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="verifyTheRedirectTypeAfterMove"/>
        <!--Verify before move Redirect Path displayed with associated Target Path and Redirect Type-->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="openUrlRewriteGridFilters2"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="fillCategoryUrlKey2"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="clickOrderApplyFilters2"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForPageToLoad5"/>
        <actionGroup ref="AdminSearchByRequestPathActionGroup" stepKey="verifyTheRedirectTypeAfterMove1">
            <argument name="redirectPath" value="{{FirstLevelSubCat.name_lwr}}2/{{SubCategory.name_lwr}}/{{SimpleSubCategory.name_lwr}}.html" />
            <argument name="redirectType" value="Permanent (301)" />
            <argument name="targetPath" value="{{FirstLevelSubCat.name_lwr}}2/{{SimpleSubCategory.name_lwr}}.html"/>
        </actionGroup>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="verifyTheRequestPathAfterMove1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="verifyTheTargetPathAfterMove1"/>
        <!--Verify before move Redirect Path directs to the category page-->
        <amOnPage url="{{FirstLevelSubCat.urlKey}}2/{{SubCategory.urlKey}}/{{SimpleSubCategory.urlKey}}.html" stepKey="openCategoryStoreFrontPage"/>
        <waitForPageLoad stepKey="waitForCategoryPageToLoad"/>
        <actionGroup ref="StorefrontAssertCategoryNameIsShownInMenuActionGroup" stepKey="seeCategoryOnStoreNavigationBar">
            <argument name="categoryName" value="{{FirstLevelSubCat.name}}"/>
        </actionGroup>
        <actionGroup ref="StorefrontAssertCategoryTitleActionGroup" stepKey="seeCategoryInTitle">
            <argument name="categoryTitle" value="{{SimpleSubCategory.name}}"/>
        </actionGroup>
    </test>
</tests>
