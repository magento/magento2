<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminUpdateCategoryAndCheckDefaultUrlKeyOnStoreViewTest">
        <annotations>
            <stories value="Update categories"/>
            <title value="Update category, check default URL key on the custom store view"/>
            <description value="Login as admin and update category and check default URL Key on custom store view"/>
            <testCaseId value="MC-6063"/>
            <severity value="BLOCKER"/>
            <group value="Catalog"/>
            <group value="mtf_migrated"/>
            <group value="cloud"/>
        </annotations>
        <before>
            <actionGroup ref="AdminLoginActionGroup" stepKey="loginToAdminPanel"/>
            <createData entity="NewRootCategory" stepKey="rootCategory"/>
            <createData entity="SimpleRootSubCategory" stepKey="category">
                <requiredEntity createDataKey="rootCategory"/>
            </createData>
        </before>
        <after>
            <actionGroup ref="DeleteCustomStoreActionGroup" stepKey="deleteCustomStore">
                <argument name="storeGroupName" value="customStore.name"/>
            </actionGroup>
            <actionGroup ref="CliIndexerReindexActionGroup" stepKey="reindex">
                <argument name="indices" value=""/>
            </actionGroup>
            <deleteData createDataKey="rootCategory" stepKey="deleteRootCategory"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>
        </after>

        <!--Open Store Page -->
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="amOnAdminSystemStorePage"/>

        <!--Create Custom Store -->
        <actionGroup ref="CreateCustomStoreActionGroup" stepKey="selectCreateStore">
            <argument name="website" value="{{_defaultWebsite.name}}"/>
            <argument name="store" value="{{customStore.name}}"/>
            <argument name="rootCategory" value="{{NewRootCategory.name}}"/>
        </actionGroup>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="fillStoreName"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="fillStoreCode"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="selectStoreStatus"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="clickSaveStoreButton"/>

        <!--Create Store View-->
        <actionGroup ref="AdminCreateStoreViewActionGroup" stepKey="createStoreView">
            <argument name="StoreGroup" value="customStore"/>
            <argument name="customStore" value="customStore"/>
        </actionGroup>
        <actionGroup ref="CliIndexerReindexActionGroup" stepKey="reindex">
            <argument name="indices" value=""/>
        </actionGroup>

        <!--Update Category-->
        <actionGroup ref="AdminOpenCategoryPageActionGroup" stepKey="openAdminCategoryIndexPage"/>
        <actionGroup ref="AdminExpandCategoryTreeActionGroup" stepKey="clickOnExpandTree"/>
        <actionGroup ref="AdminCategoriesOpenCategoryActionGroup" stepKey="selectCategory">
            <argument name="category" value="$category$"/>
        </actionGroup>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForPageToLoad"/>
        <actionGroup ref="AdminChangeCategoryNameActionGroup" stepKey="updateCategoryName"/>
        <actionGroup ref="AdminSaveCategoryActionGroup" stepKey="saveUpdatedCategory"/>
        <actionGroup ref="AssertMessageInAdminPanelActionGroup" stepKey="seeSuccessMessage">
            <argument name="message" value="You saved the category."/>
        </actionGroup>
        <waitForPageLoad stepKey="waitForPageToLoad1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="scrollToSearchEngineOptimization1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="selectSearchEngineOptimization1"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="waitForPageToLoad2"/>
        <actionGroup ref="AdminAssertProperCategoryUrlActionGroup" stepKey="seeCategoryUrlKey">
            <argument name="categoryUrlKey" value="{{SimpleRootSubCategory.name_lwr}}2"/>
        </actionGroup>
        <!--Open Category in Store Front  Page-->
        <amOnPage url="/{{NewRootCategory.urlKey}}/{{_defaultCategory.urlKey}}.html" stepKey="seeTheCategoryInStoreFront"/>
        <waitForPageLoad stepKey="waitForCategoryPageToLoad"/>
        <comment userInput="Comment is added to preserve the step key for backward compatibility" stepKey="clickSwitchStoreButtonOnDefaultStore"/>
        <actionGroup ref="StorefrontSwitchStoreActionGroup" stepKey="selectSecondStoreToSwitchOn">
            <argument name="storeName" value="{{customStore.name}}"/>
        </actionGroup>
        <actionGroup ref="StorefrontAssertCategoryNameIsShownInMenuActionGroup" stepKey="seeUpdatedCatergoryInStoreFront">
            <argument name="categoryName" value="{{_defaultCategory.name}}"/>
        </actionGroup>
        <click selector="{{StorefrontHeaderSection.NavigationCategoryByName(_defaultCategory.name)}}" stepKey="selectCategoryOnStoreFront"/>
        <waitForPageLoad stepKey="waitForProductToLoad"/>
        <seeElement selector="{{StorefrontCategoryMainSection.CategoryTitle(_defaultCategory.name)}}" stepKey="seeTheUpdatedCategoryTitle"/>
    </test>
</tests>
