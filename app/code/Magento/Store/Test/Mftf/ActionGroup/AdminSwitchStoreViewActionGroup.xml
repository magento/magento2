<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->
<actionGroups xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/actionGroupSchema.xsd">
    <actionGroup name="AdminSwitchBaseActionGroup">
        <arguments>
            <argument name="storeView" defaultValue="customStore.name"/>
        </arguments>
        <click selector="{{AdminMainActionsSection.storeViewDropdown}}" stepKey="clickStoreViewSwitchDropdown"/>
        <waitForElementVisible selector="{{AdminConfirmationModalSection.ok}}" stepKey="waitingForInformationModal"/>
        <click selector="{{AdminConfirmationModalSection.ok}}" stepKey="confirmStoreSwitch"/>
        <waitForPageLoad stepKey="waitForStoreViewSwitched"/>
        <see userInput="{{storeView}}" selector="{{AdminMainActionsSection.storeSwitcher}}" stepKey="seeNewStoreViewName"/>
    </actionGroup>

    <actionGroup name="AdminSwitchStoreViewActionGroup" extends="AdminSwitchBaseActionGroup">
        <waitForElementVisible selector="{{AdminMainActionsSection.storeViewByName(storeView)}}" after="clickStoreViewSwitchDropdown" stepKey="waitForStoreViewsAreVisible"/>
        <click selector="{{AdminMainActionsSection.storeViewByName(storeView)}}" after="waitForStoreViewsAreVisible" stepKey="clickStoreViewByName"/>
    </actionGroup>

    <actionGroup name="AdminSwitchWebsiteActionGroup" extends="AdminSwitchBaseActionGroup">
        <waitForElementVisible selector="{{AdminMainActionsSection.websiteByName(storeView)}}" after="clickStoreViewSwitchDropdown" stepKey="waitForStoreViewsAreVisible"/>
        <click selector="{{AdminMainActionsSection.websiteByName(storeView)}}" after="waitForStoreViewsAreVisible" stepKey="clickStoreViewByName"/>
        <click selector="{{AdminConfirmationModalSection.ok}}" stepKey="confirmStoreSwitch"/>
    </actionGroup>
</actionGroups>
